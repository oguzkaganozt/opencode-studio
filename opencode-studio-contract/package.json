{
  "name": "opencode-studio-contract",
  "version": "1.0.0",
  "private": true,
  "description": "OpenCode Studio Contract — specification, reference studio, and conformance suite",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build:reference": "bun --cwd reference-studio install && bun --cwd reference-studio run build",
    "test:conformance": "bun run conformance/src/run.ts",
    "test:conformance:studios": "bun run conformance/src/run.ts --studios",
    "test:conformance:all": "bun run test:conformance && bun run test:conformance:studios"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
